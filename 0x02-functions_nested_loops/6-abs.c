#include "main.h"
/**
* _abs(int) - it computes the absolute value of an integer
* @n: parameter
* Return:absolute valueof a number
*/

int _abs(int n)
{
	int n;
	if (n < 0)
		n = -n;
	return (n);
}

